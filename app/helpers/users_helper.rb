# frozen_string_literal: true

module UsersHelper
  # def link_to_remove_fields(name, f)
  #     f.hidden_field(:_destroy) + link_to(name, "remove_fields(this)")
  # end

  # def link_to_add_user(name, f, association)
  #     # new_object = User.new
  #     # fields = f.fields_for(association, new_object, :child_index => "new_user") do |builder|
  #     #   render("user_fields", :f => builder)
  #     # end
  #     link_to_function(name, "add_fields(this, \"user\", \"#{escape_javascript(fields)}\")")
  # end
end
