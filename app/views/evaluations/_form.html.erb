<%= form_with(model: evaluation, local: true) do |form| %>
  <% if evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(evaluation.errors.count, "error") %> prohibited this evaluation from being saved:</h2>

      <ul>
        <% evaluation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :authorUserId %>
    <%= form.number_field :authorUserId %>
  </div>

  <div class="field">
    <%= form.label :teamId %>
    <%= form.number_field :teamId %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="field">
    <%= form.label :assignmentId %>
    <%= form.number_field :assignmentId %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
